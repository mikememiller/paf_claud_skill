# Syntax Corporation © 2026 — EBS AP PAF runtime dependencies
#
# The ONLY hard dependency for the live path is python-oracledb (thick mode).
# Everything in the default pipeline (deterministic extractor, policy engine,
# QA gate, CSV writer) is pure Python standard library.
#
# The packages below the marker are OPTIONAL — needed only for the LLM
# extractor and the MCP server transport. The agent runs end-to-end without
# them as long as EBSDB is reachable.

python-oracledb>=2.0.0

# --- optional ---------------------------------------------------------------
# anthropic>=0.40.0      # LLMExtractor (only when ANTHROPIC_API_KEY is set)
# mcp>=1.0.0             # FastMCP server transport
# pydantic>=2.0          # used by FastMCP
